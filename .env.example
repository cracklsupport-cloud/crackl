# Local development
EXPO_PUBLIC_BACKEND_URL=http://localhost:3000

# Production web/mobile builds
# Use your deployed API origin, for example:
# EXPO_PUBLIC_BACKEND_URL=https://api.crackl.app

# Backend-only semantic answer judge lives in CRACKL-backend/.env.
# Never expose OPENAI_API_KEY through EXPO_PUBLIC_* variables.

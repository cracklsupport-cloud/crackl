import { useState, useEffect } from 'react';

/**
 * Temporarily locked to 0 to permanently show the updated AuthThemeDefault
 * instead of alternating every 12 hours.
 */
export function useLoginTheme() {
  return 0;
}
